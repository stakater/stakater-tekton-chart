{{- if .Values.pipelines.enabled }}
apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: {{ include "pipeline-charts.name" . }}
  namespace: {{ .Release.Namespace }}
spec:
  {{- $default_tasks := list }}
  {{- range $path, $_ :=  .Files.Glob  "default-config/tasks/**.yaml" }}
      {{- $task_file := $.Files.Get $path | fromYaml }}
      {{- $default_tasks = append $default_tasks $task_file }}
  {{- end }}
  {{- $pipeline_params := include "pipeline-charts.get_pipeline_params" (list .Values.pipelines.tasks .Values.pipelines.finally $default_tasks ) | fromJson }}
  {{- if $pipeline_params }}
  params:
    {{- range $k,$v := $pipeline_params }}
    - name: {{ $k }}
      type: string
    {{- end }}
  {{- end }}
  {{- if .Values.workspaces }}
  workspaces:
    {{- range $.Values.workspaces }}
    - name: {{ .name }}
    {{- end }}
  {{- end }}
  {{- if .Values.pipelines.finally }}
  finally:
    {{- range .Values.pipelines.finally }}
    {{- $default := include "pipeline-charts.get_default_task_values" (list . $default_tasks ) | fromJson }}
    {{- $task_params := include "pipeline-charts.merge_params" (list $default.params .params false ) }} 
    {{- $task_workspaces := include "pipeline-charts.merge_workspaces" (list $default.workspaces .workspaces false) }} 
    {{- $task_params_name_only := include "pipeline-charts.merge_params" (list $default.params .params true) }} 
    {{- $task_workspaces_name_only := include "pipeline-charts.merge_workspaces" (list $default.workspaces .workspaces true) }} 
    - name: {{ .name | default .default_task_name }}
      {{- if or .when $default.when }}
      when:
      {{- .when | default $default.when | toYaml | nindent 6 }}
      {{- end }}
      {{- if and .taskSpec .name }}
      taskSpec:
        {{- .taskSpec | toYaml | nindent 8 }}
        {{- if .params }}
        params:
        {{- range .params }}
        - name: {{ .name }}
        {{- end }}
        {{- end }}
        {{- if .workspaces }}
        workspaces:
        {{- range .workspaces }}
        - name: {{ .name }}
        {{- end }}
        {{- end }}
      {{- else if and $default.taskSpec .default_task_name }}
      taskSpec:
        {{- $default.taskSpec | toYaml | nindent 8 }}
        {{- if $task_params }}
        params: {{ $task_params_name_only  | nindent 8 }}
        {{- end }}
        {{- if $task_workspaces }}
        workspaces: {{ $task_workspaces_name_only | nindent 8 }}
        {{- end }}
      {{- else if and .taskRef .name }}
      taskRef:
          name: {{ .taskRef.task }}
          kind: {{ .taskRef.kind }}
      {{- else if and $default.taskRef .default_task_name }}
      taskRef:
          name: {{ $default.taskRef.task }}
          kind: {{ $default.taskRef.kind }}
      {{- end }}
      {{- if .taskSpec }}
      {{- if .params }}
      params:
      {{- range .params }}
      - name: {{ .name }}
        {{- if .value }}
        value: {{ .value | quote }}
        {{- else }}
        value: $(params.{{.name}})
        {{- end }}
      {{- end }}
      {{- end }}
      {{- if .workspaces }}
      workspaces:
      {{- range .workspaces }}
      - name: {{ .name }}
        workspace: {{ .workspace }}
      {{- end }}
      {{- end }}
      {{- else }}
      {{- if ne $task_params "[]" }}
      params: {{ $task_params | nindent 6 }}
      {{- end }}
      {{- if ne $task_workspaces "[]" }}
      workspaces: {{ $task_workspaces | nindent 6 }}
      {{- end }}
      {{- end }}
    {{- end }}
  {{- end }}
  tasks:
    {{- $previoustask := "" }}
    {{- range .Values.pipelines.tasks }}
    {{- $default := include "pipeline-charts.get_default_task_values" (list . $default_tasks ) | fromJson -}}
    {{- $task_params := include "pipeline-charts.merge_params" (list $default.params .params false ) }} 
    {{- $task_workspaces := include "pipeline-charts.merge_workspaces" (list $default.workspaces .workspaces false) }} 
    {{- $task_params_name_only := include "pipeline-charts.merge_params" (list $default.params .params true) }} 
    {{- $task_workspaces_name_only := include "pipeline-charts.merge_workspaces" (list $default.workspaces .workspaces true) }} 
    - name: {{ .name | default .default_task_name }}
      {{- if or .when $default.when }}
      when: 
      {{- .when | default $default.when | toYaml | nindent 6 }}
      {{- end }}
      {{- if and .taskSpec .name }}
      taskSpec:
        {{- .taskSpec | toYaml | nindent 8 }}
        {{- if .params }}
        params:
        {{- range .params }}
        - name: {{ .name }}
        {{- end }}
        {{- end }}
        {{- if .workspaces }}
        workspaces:
        {{- range .workspaces }}
        - name: {{ .name }}
        {{- end }}
        {{- end }}        
      {{- else if and $default.taskSpec .default_task_name }}
      taskSpec:
        {{- $default.taskSpec | toYaml | nindent 8 }}
        {{- if ne $task_params "[]" }}
        params: {{ $task_params_name_only  | nindent 8 }}
        {{- end }}
        {{- if ne $task_workspaces "[]" }}
        workspaces: {{ $task_workspaces_name_only | nindent 8 }}
        {{- end }}
      {{- else if and .taskRef .name }}
      taskRef:
          name: {{ .taskRef.task }}
          kind: {{ .taskRef.kind }}
      {{- else if and $default.taskRef .default_task_name }}
      taskRef:
          name: {{ $default.taskRef.task }}
          kind: {{ $default.taskRef.kind }}
      {{- end }}
      {{- if .retries }}
      retries: {{ .retries }}
      {{- end }}
      {{- if .taskSpec }}
      {{- if .params }}
      params:
      {{- range .params }}
      - name: {{ .name }}
        {{- if .value }}
        value: {{ .value | quote }}
        {{- else }}
        value: $(params.{{.name}})
        {{- end }}
      {{- end }}
      {{- end }}
      {{- if .workspaces }}
      workspaces:
      {{- range .workspaces }}
      - name: {{ .name }}
        workspace: {{ .workspace }}
      {{- end }}
      {{- end }}
      {{- else }}
      {{- if ne $task_params "[]" }}
      params: {{ $task_params | nindent 6 }}
      {{- end }}
      {{- if ne $task_workspaces "[]" }}
      workspaces: {{ $task_workspaces | nindent 6 }}
      {{- end }}
      {{- end }}
      {{- if or $previoustask .runAfter }}
      runAfter:
        {{- if .runAfter }}
        {{- range .runAfter }}
        - {{ . }}
        {{- end }}
        {{- else }}
        {{- if $previoustask }}
        - {{ $previoustask }}
        {{- end }}
        {{- end -}}
      {{- end }}
      {{- $previoustask = (.name | default .default_task_name) }}
    {{- end }}
{{- end }}
